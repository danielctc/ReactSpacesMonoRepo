// This component has been commented out in favor of using SpaceHLSStreamController
// which provides more space-specific functionality.

// Export an empty component to prevent import errors
const HLSStreamController = () => null;
export default HLSStreamController; 